# connectors-sdk is pinned to a COMMIT, not @master: master moves under us, it is the
# one dependency no CVE scanner can audit, and an unannounced pycti bump there broke
# `pip install` outright. pycti must match the exact pin that commit declares (the SDK
# pins pycti==), so bump these two lines together and deliberately.
pycti==7.260902.0
connectors-sdk @ git+https://github.com/OpenCTI-Platform/connectors.git@5a48f42b6e73c2ea18ff4393c6207c954c840773#subdirectory=connectors-sdk
pydantic>=2.8.2,<3.0.0
pydantic-settings>=2.5,<3.0
PyYAML==6.0.2
requests==2.33.1
stix2==3.0.1
validators==0.35.0